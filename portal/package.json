{
  "name": "tpc-portal",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Login + personal file upload portal (Cognito + S3, direct-to-S3).",
  "scripts": {
    "dev": "vite",
    "build": "rm -rf assets && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity": "^3.658.0",
    "@aws-sdk/client-s3": "^3.658.0",
    "@aws-sdk/s3-request-presigner": "^3.658.0",
    "amazon-cognito-identity-js": "^6.3.12"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
