# AIPost247 Python dependencies — installed automatically by run.py / run.sh.
requests>=2.31
python-dotenv>=1.0
schedule>=1.2

# NOTE:
#  * The default content generator is the Gemini CLI (npm: @google/gemini-cli),
#    which AIPost247 installs for you and which you log into with Google — no
#    Python package and no API key required.
#  * 'openai' is installed on demand ONLY if you pick the OpenAI provider in
#    `python run.py setup`.
