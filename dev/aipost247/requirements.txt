# AIPost247 Python dependencies — installed automatically by run.py / run.sh.
requests>=2.31
python-dotenv>=1.0
schedule>=1.2

# NOTE:
#  * The default content generator is Google Antigravity CLI (`agy`), installed
#    on demand and authenticated with Google — no Python package or API key.
#  * Gemini CLI remains available for supported accounts.
#  * 'openai' is installed on demand ONLY if you pick the OpenAI provider in
#    `python run.py setup`.
