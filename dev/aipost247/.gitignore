# Secrets — NEVER commit these.
.env
*.env
!.env.example

# Local runtime state
data/
logs/
*.db
*.sqlite3

# Runtime memory the app generates per user — never commit (seed files are kept).
memory/business.md
memory/skill.md
memory/steering.md

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
.venv/
venv/
env/

# OS noise
.DS_Store

# Build output: keep download/aipost247.zip, but never commit an EXTRACTED copy
# of it (created by unzipping in place to test). The zip is the only artifact.
/download/aipost247/
